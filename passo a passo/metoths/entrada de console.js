let name = window.prompt("What is your name?", "Digite seu nome"); // seria o console.parse (int) = (seria para )
name = name ? name : "ymous";

let age = prompt("Hello " + name + " how old are you?");
alert(name + " is " + age + " years old");